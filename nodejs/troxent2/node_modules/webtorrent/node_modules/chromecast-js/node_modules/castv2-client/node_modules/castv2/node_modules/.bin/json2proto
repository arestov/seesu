#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../protobufjs/bin/json2proto" "$@"
  ret=$?
else 
  node  "$basedir/../protobufjs/bin/json2proto" "$@"
  ret=$?
fi
exit $ret
