#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../raven/bin/raven" "$@"
  ret=$?
else 
  node  "$basedir/../raven/bin/raven" "$@"
  ret=$?
fi
exit $ret
